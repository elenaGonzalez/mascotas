{% extends "::base.html.twig" %}
{% block body %}
<div class="text-center">
    <h1>Finalizar publicación</h1>
    <div class="text-center" style="align:center">
        <h1 class="text-info">Publicación</h1>
        <table class="container record_properties table-striped table-bordered text-center">
            <tbody>
            <h1>Metodo: {{metodo}} entidad: {{publicacion}}</h1>
                <tr class="text-center alert alert-danger">
                    <th class="text-center">Tipo de Publicación</th>
                    <th class="text-center">Fecha de publicación<i class = "icon-calendar" ></i></th>
                    <th class="text-center">Aviso</th>
                    <th class="text-center"><i class="icon-picture"></i>Foto</th>
                    <th class="text-center">Contacto</th>
                </tr>
                <tr>
                 {% if ( publicacion.tipo ) =='e' %}
                    <td class="text-center"><strong>Encontrado</strong></td>
                 {% else %}
                    <td class="text-center"><strong>Perdido</strong></td>
                 {% endif %}
                    <td class="text-center">{{ publicacion.fechapublicacion|date('d/m/Y') }}</td>
                    <td class="text-center">{{ publicacion.aviso }}</td>
                    <td class="text-center"><img src="{{ asset(publicacion.foto.webpath) }}" alt="Sin foto" width="140" height="140" class="img-rounded"></td>
                    <td class="text-center">{{ publicacion.contacto }}</td>
                    </tr>
                </tbody>
            </table>
         <form action="{{ path('publicacion_finalizar', {'id': publicacion.id }) }}" method="post" {{ form_enctype(finalizar_form) }}>
            {{ form_widget(finalizar_form) }}
            <button type="submit" class="btn-danger">Finalizar</button>
         </form>
        </div>
{% endblock %}